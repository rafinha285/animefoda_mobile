import { StyleSheet } from 'react-native';
import {body_purple, white} from "./baseValues.ts";

export const homeStyle = StyleSheet.create({
    body:{
        backgroundColor:body_purple,
        margin:0,
        // flex:1
    },
})

import { StyleSheet } from 'react-native';
import {white} from "./baseValues.ts";

export const theme = {
    style: {
        fontFamily: 'Roboto-Medium',
        color: '#ffffff',
    },
};
export const baseStyles = StyleSheet.create({
    font:{
        fontFamily: 'Roboto-Medium',
        color:white,
    }
})
